#include "TcpServer.h"

#include <iostream>
#include <string>


int main(int argc, char* argv[]) {
    std::string _ip = "0.0.0.0";
    int _port = 80;

    if (argc < 2) {
        //file error
	} else if (argc == 2) {
        //server address error
    } else if(argc == 3) {
        //port error
    }

    //get file text
    
    //check file path and name

    //check ip and port

    //push it to address    
    
    return 0;
}
